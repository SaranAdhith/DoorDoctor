/**
 * Clinical components (§4.2-4.9).
 *
 * Composed from `components/ui/` like everything else — Phase 2's rule does not
 * stop applying because a screen is clinical.
 */

export { EmergencyBlock } from './EmergencyBlock'
export { EscalationTimeline } from './EscalationTimeline'
export { LabResultTable } from './LabResultTable'
export { Phq2Form } from './Phq2Form'
export { SafetyScoreCard, SafetyScoreCardSkeleton } from './SafetyScoreCard'
